# History Pieces 본선 1차 통합본 감사 보고서

> 후속 개선 상태(2026-07-17): 이 문서는 수정 전 통합본에서 발견한 문제를 기록한 최초 감사본이다. 아래의 중국어 전체 UI 미완성, 중국인 모드 포인트 색, 추천 선택 흐름, 여정필름 책임 분산 문제는 현재 개선본에서 조치되었으며 결과는 `History_Pieces_1차통합본_개선보고서.md`에 정리했다.

> 이 문서는 수정 전 1차 통합본을 기준으로 작성한 최초 감사 기록입니다. 이후 반영된 구조·코드 변경과 최신 검증 결과는 `History_Pieces_1차통합본_개선보고서.md`를 기준으로 확인합니다.

- 기준일: 2026-07-17
- 기준 버전: 초기 디자인 시안 공유 직전의 공통 `index.html`, `script.js`, `style.css`
- 검토 범위: 공통 3개 파일, 구도훈 AI 모듈, 최유석 생성형 AI·웹툰·추천 모듈 및 JSON, 이지영 문구·여정필름 자료, 예선 HTML·JS·CSS, 본선 구현 범위와 일정
- 현재 단계: 더미·Fallback을 포함해 전체 흐름을 검증하는 본선 1차 통합본

## 1. 프로젝트 목적과 방향 이해

History Pieces는 관광객이 목포역 현장에서 세 개의 이야기 조각을 사진으로 찾고, AI 판별과 과거·현재 기록 오버레이를 거쳐 장소의 역사적 맥락을 웹툰으로 해금한 뒤, 직접 남긴 5초 영상 세 개를 여정필름으로 완성하는 AI 스토리 관광 서비스다.

핵심은 단순 장소 인증이나 번역이 아니다.

1. 사용자가 현재 장소를 직접 기록한다.
2. 사진 속 단서를 AI가 판별한다.
3. 현재 사진과 옛 기록을 겹쳐 과거와 현재를 연결한다.
4. 검증된 사실과 사용자의 닉네임·언어·문화권을 반영한 이야기를 생성한다.
5. 세 조각을 목포역 전체 서사로 묶는다.
6. 사용자의 5초 기록 세 개를 여정필름으로 남긴다.
7. 주변 근대문화 장소로 다음 이동을 연결한다.

예선은 이야기 조각 하나를 이용해 이 경험을 빠르게 증명한 프로토타입이고, 본선은 같은 경험을 세 조각으로 확장하고 직접 학습 AI, 중국인 맞춤 해설, 생성형 AI, 여정필름, 추천 및 실패 대응을 고도화한 버전이다. 따라서 예선의 오버레이는 본선에서도 유지되는 핵심 경험이다.

### 확정 본선 흐름

시작 → 언어 선택 → 국가 선택(한국/중국) → 닉네임 → 첫 사진 → 목포역 확인 → 기록새 등장 → 조각 1 촬영·AI 판별·오버레이·4패널 웹툰·5초 기록 → 조각 2 동일 흐름 → 조각 3 동일 흐름 → 장소 이야기 해금 → 장소 이야기 웹툰 → 퀴즈 → 여정필름 → 다운로드·SNS → 다음 장소 추천·외부 URL

이 이해는 현재 기획과 일치한다. 프로젝트 목적을 바꾸는 재설계는 하지 않았다.

## 2. 현재 구현 총평

현재 공통틀은 1차 통합본으로 적절하다. 33개 화면, 단일 `appState`, `data-action` 기반 이벤트 라우팅, 팀원 모듈 연결 지점, 샘플 진행, 생성형 AI Fallback, 세 조각·세 기록·장소 이야기·퀴즈·여정필름·추천까지의 전체 경로가 존재한다. 실제 기능이 아직 없는 영역도 흐름을 막지 않도록 되어 있어 1차 목표인 “더미 데이터로 처음부터 끝까지 진행”은 충족한다.

다만 현재 코드를 최종 구현으로 설명해서는 안 된다. 직접 학습 AI, 첫 장소 탐색 AI, 조각 2·3의 옛 기록 자산, 실제 생성형 AI 서버, 여정필름 다운로드 Fallback, 최종 예약 URL은 아직 완성 상태가 아니다.

## 3. 요구사항 반영 상태

| 요구사항 | 현재 상태 | 판정 |
| --- | --- | --- |
| 목포역 한 장소 집중 | 목포역과 세 조각으로 고정 | 충족 |
| 언어 선택 | 한국어·중국어 선택 가능, 목록 스크롤 가능 | 1차 충족 |
| 국가·문화권 선택 | 한국·중국만 활성화, 나머지는 비활성 표시 | 충족 |
| 닉네임 입력·반영 | 상태·웹툰·파일명에 반영, 20자 제한 | 충족 |
| 첫 사진 기록·목포역 확인 | 업로드·샘플·확인·재업로드 흐름 | 흐름 충족, 탐색 AI는 더미 |
| 기록새 등장 | 화면 전체 클릭으로 대사 진행 | 충족 |
| 조각 1·2·3 | 각 미션·업로드·AI 결과·웹툰·5초 기록 | 충족 |
| 직접 학습 AI | 인터페이스·반환 형식·실패 UI 존재 | 실제 모델 미완성 |
| 과거·현재 오버레이 | 세 조각 공통 화면과 순서 연결 | 조각 1 충족, 2·3 자산 대기 |
| 4패널 조각 웹툰 | 각 조각 4패널 DOM과 JSON 렌더링 | 충족 |
| 장소 이야기 웹툰 | 4패널과 Fallback | 충족 |
| 중국인 맞춤 해설 | 문화권 상태·중국어 문구·추천 이유 분기 | 부분 충족 |
| 5초 기록 3개 | 영상 선택·촬영·미리보기·상태 저장 | 충족 |
| 여정필름 | 브라우저 합성·순차 재생 Fallback | 부분 충족 |
| 다운로드·SNS | 합성 성공 시 다운로드, Instagram 연결 | 부분 충족 |
| 다음 장소 추천 | 검증 추천 1건·지도·상세 URL | 충족 |
| 예약 URL | 추천 데이터의 예약 URL이 `null` | 미충족·기획 결정 필요 |
| 발표용 샘플 진행 | 사진·AI·영상별 샘플 경로와 초기화 | 충족 |
| 최종 디자인 | 이번 감사 범위에서 의도적으로 제외 | 3차 대상 |

## 4. 감사 결과와 반영 수정

### 4.1 본선 필수 오버레이 누락

- 위치: 기존 공통 흐름의 각 `piece-N-ai-result-page` 다음 단계
- 원인: 본선을 예선과 다른 흐름으로 해석하면서 예선 오버레이를 제외 대상으로 판단할 가능성이 있었다.
- 영향: “현재 기록이 옛 기록과 만나 장소 이야기를 갱신한다”는 서비스 핵심 경험과 퀴즈 문구가 분리된다.
- 수정 이유: 사용자가 오버레이를 본선에서도 유지한다고 명확히 확정했다.
- 수정 내용: 세 조각이 모두 `AI 성공 → piece-overlay-page → 웹툰`으로 이동하도록 공통 오버레이 화면 하나를 재사용했다. 출처가 확인된 실제 옛 기록 사진이 전달되기 전에는 세 조각 모두 자산을 추측하지 않고 “옛 기록 자산 연결 대기” 상태를 표시한다.

### 4.2 설정 목록의 스크롤·선택 접근성 부족

- 위치: `index.html` 언어·국가 목록, `style.css` `.list-card`
- 원인: 목록이 화면 높이를 넘을 때 별도 스크롤 규칙과 라디오 역할이 없었다.
- 영향: 작은 모바일 화면에서 아래 항목 확인이 어렵고, 지원하지 않는 선택지가 보조기기에 명확히 전달되지 않는다.
- 수정 이유: 아이폰 초기 설정처럼 스크롤 가능한 목록이라는 확정 UX 방향을 최소 구조로 지원해야 한다.
- 수정 내용: 목록 내부 세로 스크롤과 터치 스크롤을 적용하고, 네이티브 버튼에 `aria-pressed`·`aria-disabled` 상태를 연결했다.

### 4.3 문화권에 따른 포인트 색 변경

- 위치: 기존 `style.css`의 중국 문화권 전용 accent 재정의
- 원인: 중국 모드를 시각적으로 구분하려고 별도 색을 적용했다.
- 영향: “한국 모드와 중국인 모드의 포인트 컬러는 동일”이라는 확정 요구와 충돌한다.
- 수정 이유: 문화권 차이는 해설 관점과 콘텐츠로 보여줘야 하며 색 변경은 확정 기획이 아니다.
- 수정 내용: 중국 모드 accent 재정의를 제거하고 동일한 브라운·골드 계열을 사용한다.

### 4.4 초기화 후 중국 모드 소개 문구가 이전 언어로 남는 상태 오류

- 위치: `script.js`의 `COPY_DATA`, `applyCultureCopy()`, `resetDemo()`
- 원인: 한국어 데이터에 중국 문화권 소개 문구가 없어, 중국어로 한 번 실행한 뒤 초기화하고 한국어·중국 문화권을 선택하면 이전 중국어 DOM이 남을 수 있었다.
- 영향: 반복 시연에서 언어 상태와 화면 문구가 불일치한다.
- 수정 이유: 본선 시연은 같은 기기에서 여러 번 초기화해 실행할 가능성이 높다.
- 수정 내용: 제공된 한국어 중국 모드 소개 문구를 한국어 데이터에도 포함하고 초기화 때 항상 다시 적용한다.

### 4.5 재촬영·재업로드 때 이전 미디어 상태 잔존

- 위치: `resetFirstPhoto()`, `resetPiecePhotoForRetry()`, 사진·영상 교체 처리
- 원인: 화면만 되돌아가고 이전 파일, 미리보기 URL, AI 결과가 남을 수 있었다.
- 영향: 사용자는 새 사진을 올리지 않았는데 분석 버튼이 활성화되거나 이전 판별 결과가 다음 시도에 섞일 수 있다. 장시간 반복 시 Object URL도 누적될 수 있다.
- 수정 이유: 재시도는 해당 단계 상태를 깨끗하게 되돌려야 한다.
- 수정 내용: 파일 입력·미리보기·상태·AI 결과를 초기화하고, 교체·초기화 때 Object URL을 해제한다.

### 4.6 긴 닉네임으로 인한 레이아웃·파일명 위험

- 위치: `nickname-input`, `saveNickname()`
- 원인: 입력 길이 제한이 없었다.
- 영향: 웹툰 대사, 제목, 다운로드 파일명에서 레이아웃이 무너질 수 있다.
- 수정 이유: 닉네임 의미는 유지하면서 1차 통합본의 화면 안정성을 높여야 한다.
- 수정 내용: HTML과 JavaScript 양쪽에서 유니코드 20자까지 제한하고 빈 값은 `여행자`로 처리한다.

### 4.7 모바일 화면 높이와 키보드·주소창 변화

- 위치: `style.css`의 앱 셸·페이지 높이
- 원인: `100vh`만 사용하면 모바일 주소창 크기 변화에서 화면이 잘릴 수 있다.
- 영향: 하단 버튼 접근성이 불안정해진다.
- 수정 이유: 디자인을 새로 구현하지 않고도 향후 모바일 시안 적용 기반을 안정화할 수 있다.
- 수정 내용: 기존 호환 값을 유지하면서 지원 브라우저에서는 `100dvh`를 사용한다.

### 4.8 중복 클릭·생성 중 재호출

- 위치: AI 실행, 웹툰 생성, 여정필름 합성 버튼
- 원인: 비동기 처리 중 버튼을 여러 번 누를 수 있었다.
- 영향: 화면 중복 생성, API 중복 호출, 합성 상태 충돌 가능성이 있다.
- 수정 이유: 발표 환경에서 빠른 연속 클릭은 흔한 실패 원인이다.
- 수정 내용: 공통 `runLocked()`와 생성 상태 UI로 처리 중 버튼을 잠근다.

## 5. 수정하지 않고 현재 구현을 유지한 부분

다음은 현재 단계와 프로젝트 의도에 적절하므로 억지로 변경하지 않았다.

- `appState` 키: 팀 모듈 계약과 일치하므로 유지했다.
- `data-action` 기반 단일 이벤트 라우팅: 공통본 충돌을 줄이는 구조이므로 유지했다.
- 팀원 모듈 로드 순서: `dohun-ai → script → yuseok-ai → renderer → recommendation`을 유지했다.
- 검증된 추천 1건만 노출: 확인되지 않은 후보를 억지로 보여주지 않는 현재 정책이 적절하다.
- 생성형 AI API 키를 브라우저에 두지 않는 구조: 보안상 적절하다.
- 디자인 시안의 세부 이미지·폰트·색·아이콘·뿅 등장·살랑 움직임: 시안은 구현 명세가 아니고 3차 디자인 대상이므로 이번 코드에 추가하지 않았다.
- 첫 장소 분석의 더미 로딩: 실제 모델·API가 제공되지 않은 1차 단계이므로 흐름만 유지하되 완성 기능으로 표기하지 않는다.

## 6. 예선 코드 재사용 분류

| 분류 | 예선 자산 | 본선 처리 |
| --- | --- | --- |
| 그대로 재사용 가능한 개념 | 현재·옛 기록 겹침, 현재/옛 기록/겹쳐 보기, 투명도 슬라이더, 자동 전환 | 공통 오버레이 컴포넌트로 유지 |
| 일부 수정 후 재사용 | 오버레이 CSS와 이미지 전환 로직 | inline 이벤트와 단일 조각 ID를 제거하고 세 조각 공통 상태에 연결 |
| 일부 수정 후 재사용 | 촬영 파일 미리보기와 Object URL 처리 | 공통 상태·해제 로직·세 조각 구조로 통합 |
| 일부 수정 후 재사용 | 5초 영상 미리보기·진행 개념 | 세 영상 상태와 여정필름 합성 흐름에 맞게 사용 |
| 본선 방향과 충돌 | 단일 조각 이후 바로 전체 결과로 가는 선형 흐름 | 세 조각 반복 흐름으로 대체 |
| 제외가 적절 | 다수의 inline `onclick`, 여러 번 감싼 `showPage`, 날짜별 코드 덧붙이기 | 공통 이벤트 라우터와 단일 `showPage`를 사용 |
| 제외가 적절 | 검증되지 않은 여러 추천 후보와 임시 저장 알림 | 검증 추천 1건 및 실제 URL 정책 유지 |
| 제외가 적절 | 도슨트·보상·브이로그 등 본선 확정 화면과 중복되는 별도 화면 | 웹툰·장소 이야기·여정필름의 현재 화면을 유지 |

예선 전체 파일을 복사하지 않고, 검증된 오버레이 경험과 미디어 처리 개념만 현재 구조에 맞게 가져온 것이 적절하다.

## 7. 해결되지 않은 핵심 위험

### 7.1 직접 학습 AI가 아직 시뮬레이션

- 위치: `dohun-ai.js`
- 근거: 조각 1의 `implementation`은 `direct_model_pending`, 실제 사진은 `Math.random()` 기반 결과다. 조각 2·3은 `demo_classifier`다.
- 영향: 현재 상태로 “직접 학습 AI가 실제 판별한다”고 발표하면 코드와 설명이 불일치한다.
- 현재 조치: 1차 통합본에서는 샘플·성공·실패 UI 검증용으로 유지한다. 실제 모델을 추측해서 만들지 않았다.

### 7.2 조각 2·3 오버레이 사료 자산 미제공

- 위치: `OVERLAY_ARCHIVE_IMAGES`
- 근거: 조각 1 경로만 예선에서 확인되었고 조각 2·3 경로·출처·정렬 기준이 없다.
- 영향: 본선 필수 오버레이 경험이 두 조각에서 현재 사진만 보여주는 Fallback 상태다.
- 현재 조치: 없는 이미지를 생성하거나 사실 자료처럼 꾸미지 않고 명확한 대기 상태를 표시한다.

### 7.3 문화권과 언어가 독립인데 Fallback은 언어만으로 선택

- 위치: `yuseok-ai-0716.js`의 `getFallbackData(language)`
- 근거: API 요청에는 `culture`가 포함되지만 Fallback 파일은 `fallback-ko.json`, `fallback-cn.json` 두 개뿐이다.
- 영향: 한국어+중국 문화권에서 한국어이지만 중국인 관점인 Fallback을 보장할 수 없다.
- 현재 조치: 모듈 계약을 임의 변경하지 않고 팀 작업으로 남긴다.

### 7.4 여정필름 Fallback은 다운로드 파일을 만들지 못함

- 위치: `journeyShowSequentialFallback()`, `journeyDownloadFilm()`
- 근거: 합성 실패 시 세 영상을 순차 표시하지만 `journeyFilm.blob`은 비어 있고 다운로드 버튼은 비활성이다.
- 영향: 합성 미지원 브라우저에서 “여정필름 다운로드” 확정 요구를 충족하지 못한다.
- 현재 조치: 존재하지 않는 합성 샘플 파일을 가정하지 않았다.

### 7.5 예약 URL 의미 충돌

- 위치: `source-facts.json`, 추천 모듈, 예약 결과 화면
- 근거: 목포근대역사관 1관은 `reservationRequired: false`, `urls.reservation: null`인데 UI는 “예약하기/예약되었습니다”로 표현한다.
- 영향: 외부 예약 URL을 연다는 확정 흐름과 현재 검증 데이터가 충돌한다.
- 현재 조치: 지도·공식 상세 URL만 실제로 연결하고, 없는 예약 URL은 만들지 않았다. 팀장의 기획 결정이 필요하다.

### 7.6 여정필름 구현 소유권·중복 위험

- 위치: 공통 `script.js`의 `journey*` 함수와 이지영이 전달한 두 가지 여정필름 코드
- 근거: 같은 전역 함수명이 서로 다른 상태 객체와 반환 방식을 사용한다.
- 영향: 외부 파일을 그대로 추가하면 마지막 로드 파일이 함수를 덮어쓰고 공통 상태와 분리될 수 있다.
- 현재 조치: 공통본에는 검증한 한 구현만 유지하며, 전달된 두 코드를 동시에 로드하지 않는다.

### 7.7 중국어 전체 화면 적용은 미완성

- 위치: 설정·분석·추천·예약 등 정적 HTML 문구
- 근거: 핵심 미션·웹툰·퀴즈·여정필름 문구는 분기되지만 모든 고정 UI가 중국어로 바뀌지는 않는다.
- 영향: 중국인 맞춤 모드가 일부 화면에서 번역 추가 수준으로 보일 수 있다.
- 현재 조치: 제공되지 않은 번역은 생성하지 않았다.

## 8. 팀원에게 바로 전달할 작업 목록

| 작업명 | 필요한 이유 | 현재 부족한 정보 | 팀원이 수행해야 하는 작업 | 우선순위 | 공모전 경쟁력 영향 |
| --- | --- | --- | --- | --- | --- |
| 조각 1 실제 직접 학습 모델 연결 | 핵심 AI 증거 | 모델 파일, 전처리, 클래스 순서, 임계값, 실제 성능 | 구도훈: 모델·호출 함수·전처리·오류 결과·밝기/거리/각도 테스트표를 공통 반환 형식으로 전달 | P0 | 매우 큼. “AI를 썼다”가 아니라 직접 학습 근거가 됨 |
| 조각 2·3 판별 범위 확정 | 발표 설명과 코드 일치 | 실제 모델인지 규칙/데모인지 미확정 | 구도훈: 조각별 구현 방식과 화면 표기, 실패 기준, 샘플을 확정 | P0 | 과장 발표 위험 제거 |
| 첫 사진 장소 후보 탐색의 최종 방식 | 현재 1.4초 타이머뿐임 | 실제 AI/API/규칙 여부 | 구도훈·최유석: 구현 여부와 입력·반환 형식을 확정. 미구현이면 발표에서 “흐름용 더미”로 명시 | P1 | 첫 AI 인상의 신뢰도에 영향 |
| 조각 2·3 옛 기록 자산 전달 | 본선 필수 오버레이 완성 | 이미지, 저작권, 출처, 촬영 구도 대응 | 최유석·이지영: 사료 이미지 경로, 출처·이용 조건, 캡션, 어떤 현재 구도와 겹치는지 전달 | P0 | 핵심 스토리 UX 완성 |
| 문화권×언어 Fallback 4조합 | 중국인 관점이 API 실패 때도 유지되어야 함 | 한국어+중국 관점, 중국어+한국 관점 데이터 | 최유석·이지영: 언어와 문화권을 분리한 Fallback 스키마와 검수 문구 전달 | P0 | 맞춤 해설 차별점 유지 |
| 실제 생성형 AI 서버 검증 | 현재 endpoint 기본값이 빈 문자열 | 서버 URL, CORS, 응답 시간, JSON 실패 사례 | 최유석: 로컬 백엔드, API 키 보관, 12초 timeout, 정상·오류·파싱 실패 결과를 본선 기기에서 테스트 | P0 | 생성형 AI 시연 신뢰도 |
| 중국어 전체 UI 검수 | 일부 정적 화면이 한국어 | 최종 중국어 copy와 용어 통일 | 이지영: 화면 ID별 중국어 문구, 글자 잘림, 기록새 말투, 역사 표현을 원어민 또는 고급 사용자 기준으로 검수 | P0 | 문화권 맞춤 완성도 |
| 여정필름 합성 실패 다운로드 | 현재 순차 재생만 가능 | 합성된 샘플 영상 파일과 사용 조건 | 이지영·이경민: `sample-journey-film` 자산 또는 다운로드 가능한 다른 확정 정책을 정하고 iOS Safari/Android Chrome에서 검증 | P0 | 마지막 하이라이트 실패 방지 |
| 예약 URL/버튼 의미 결정 | 검증 장소는 예약 불필요 | 실제 구매·예약 URL 또는 문구 변경 승인 | 최유석·이경민: 예약 가능한 후보/URL을 확정하거나 버튼을 “다음 기록으로 이동하기”로 바꾸는 기획 결정을 문서화 | P0 | 허위 연결 인상 방지 |
| 여정필름 코드 단일 소유본 확정 | 같은 전역 함수의 두 버전 존재 | 최종 채택 구현과 함수 계약 | 이지영·이경민: 한 파일만 final로 지정하고 입력·콜백·상태·Fallback·다운로드 계약을 고정 | P0 | 통합 충돌 방지 |
| 전체 자산 인벤토리 | 다수 경로가 코드에만 존재 | 실제 파일 존재 여부와 저작권 | 전 팀: 이미지·영상·모델·JSON별 파일, 출처, 라이선스, Fallback 여부를 표로 제출 | P0 | 현장 404와 저작권 질문 방어 |
| 본선 기기 반복 시연 테스트 | 브라우저 권한·코덱·오프라인 변수가 큼 | 최종 기기와 네트워크 조건 | 이경민: 초기화 후 3회 반복, 권한 거부, API 끊김, 모델 실패, 자산 누락, 다운로드를 체크리스트로 기록 | P0 | 발표 중단 위험 감소 |

## 9. AI 공모전 관점 평가

### 강점

- 사용자가 찍은 현재 기록이 AI 판별, 역사 오버레이, 문화권 맞춤 생성, 여정필름으로 이어져 AI 기능들이 하나의 관광 경험 안에 연결된다.
- 직접 학습 이미지 분류와 생성형 AI의 역할이 분리되어 설명 가능하다.
- `sourceFacts`와 `verified=true` 추천 정책은 역사 왜곡과 검증되지 않은 관광 정보 위험을 줄이는 좋은 방향이다.
- 중국 모드는 단순 번역이 아니라 철도·항구·개항도시·사람과 물자의 이동이라는 해설 관점을 별도로 두고 있다.
- 샘플·Fallback·초기화 경로가 있어 해커톤 발표 안정성을 고려했다.

### 약점

- 핵심 직접 학습 AI가 현재 코드에서는 무작위 시뮬레이션이다.
- 오버레이의 두 조각 자산이 없어 핵심 경험이 아직 한 조각만 완성되어 있다.
- 실제 API가 꺼져 있을 때 중국 문화권 관점이 언어 조합에 따라 약해질 수 있다.
- 여정필름 합성 실패 시 다운로드까지 보장하지 못한다.
- “예약”이라는 표현과 실제 검증 URL 상태가 충돌한다.

### 현재 코드만으로 개선해 반영한 부분

- 전체 흐름을 정확한 본선 순서로 고정했다.
- 예선 오버레이를 세 조각 공통 컴포넌트로 연결했다.
- 없는 자산·모델·API를 성공한 것처럼 보이지 않게 했다.
- 반복 시연의 상태 누수, 중복 클릭, 미디어 URL 누수, 설정 스크롤 문제를 줄였다.
- 문화권 색은 동일하게 유지하고 차이를 콘텐츠로 드러내도록 했다.

### 시연·발표에서 강조할 근거

1. “사진을 인식한다”보다 조각 1 모델의 데이터 수, 클래스, 임계값, 실제 테스트 결과를 먼저 제시한다.
2. AI 성공 직후 오버레이를 보여 과거 기록과 현재 방문 경험을 연결하는 서비스 의미를 설명한다.
3. 같은 조각을 한국 문화권과 중국 문화권에서 한 패널씩 비교해 해설 관점 차이를 보여준다.
4. 생성형 AI는 `sourceFacts`에 있는 검증 사실만 사용하고, 실패 시 고정 JSON으로 끝까지 진행한다고 설명한다.
5. 마지막에는 세 개의 사용자 영상이 여정필름과 다음 장소 이동으로 이어지는 장면을 보여준다.

## 10. 검증 결과

### 정적 검사

- JavaScript 5개 파일 `node --check` 통과
- JSON 3개 파일 파싱 통과
- HTML 표준 검사 `html-validate` 오류 0개
- HTML 중복 ID 0개
- 존재하지 않는 `data-next` 대상 0개
- HTML `data-action` 22종과 공통 라우터 처리 22종 일치
- CSS 중괄호 깊이 정상, 중국 모드 accent 재정의 없음
- 공통 파일 내 inline `onclick` 0개

### DOM 회귀 검사

- 화면 수 33개
- 한국어·중국어 전환 후 초기화와 재선택 정상
- 닉네임 20자 제한 정상
- 조각 1·2·3 샘플 AI 성공과 상태 저장 정상
- 조각 1 오버레이 사료 로드 경로 정상
- 조각 2·3 오버레이 명시적 Fallback 정상
- 세 조각 4패널 Fallback 웹툰 저장·렌더링 정상
- 세 영상 상태 저장 정상
- 장소 이야기 저장·렌더링 정상
- 검증 추천 1건과 지도 URL 정상
- 재촬영 및 첫 사진 재업로드 상태 초기화 정상
- 전체 초기화 후 시작 화면·상태 복구 정상

실제 카메라, 실제 모델, 실제 생성형 AI 서버, 실제 브라우저 영상 인코딩은 해당 자산과 본선 기기가 필요하므로 이번 자동 검사 범위에 포함하지 않았다.

## 11. 실행 방법

다음 구조를 유지한다.

```text
history-pieces/
├─ index.html
├─ style.css
├─ script.js
├─ modules/
│  ├─ dohun-ai.js
│  ├─ yuseok-ai-0716.js
│  ├─ yuseok-story-renderer-0716.js
│  └─ yuseok-recommendation-0716.js
└─ assets/
   ├─ data/
   │  ├─ source-facts.json
   │  ├─ fallback-ko.json
   │  └─ fallback-cn.json
   ├─ images/
   ├─ videos/
   └─ models/
```

JSON `fetch`와 카메라 기능을 위해 파일을 더블클릭하지 말고 프로젝트 루트에서 로컬 서버를 실행한다.

```bash
python -m http.server 5500
```

브라우저에서 `http://127.0.0.1:5500`으로 접속한다.

## 최종 판단

현재 구현은 “본선 1차 통합본”으로 적절하다. 전체 경로와 팀원 기능 삽입 지점은 안정적으로 구성되어 있고, 예선의 핵심 오버레이 경험도 본선의 세 조각 흐름에 복구되었다. 지금 단계에서 구조를 다시 설계할 필요는 없다.

다음 통합의 성패는 새로운 화면을 더 만드는 데 있지 않다. 조각 1 실제 모델, 조각 2·3 사료 자산, 문화권×언어 Fallback, 실제 생성형 AI 서버, 여정필름 다운로드 Fallback, 예약 URL 의미를 확정해 현재 자리를 실제 기능으로 교체하는 데 있다.
