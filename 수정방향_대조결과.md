# History Pieces 수정 방향 대조 결과

기준 문서: `붙여넣은 마크다운(1)(6).md`  
원본: `HistoryPieces_Fixed_Webtoon(1).zip`

## 핵심 대조

| 수정 방향 | 원본 상태 | 이번 반영 |
| --- | --- | --- |
| QR 진입 후 언어 선택부터 시작 | 별도 시작 화면이 최초 활성화 | 언어 선택 화면을 최초 화면으로 변경 |
| 한국어·중국어만 제공 | 미지원 언어·국가가 목록에 남음 | 미지원 항목을 본선 화면에서 숨김 |
| 언어 → 문화권 → 닉네임 → 기록새 | 중국 선택 시 중국 모드 소개, 닉네임 후 첫 사진 화면 | 두 문화권 모두 닉네임으로 이동하고 기록새로 연결 |
| 3개 장소 여정 | 구버전 목포역 내부 3단서 코드가 혼재 | 목포역 → 목포 대중음악의 전당 → 목포근대역사관 2관으로 통일 |
| TimeTrace 전 현재 명칭 / 이후 과거 명칭 | Detection 결과부터 과거 명칭 표시 | Detection은 현재 명칭, TimeTrace 이후는 목포역·호남은행 목포지점·동양척식주식회사 목포지점 표시 |
| 장소 Detection 오류는 실패 처리 | 오류 발생 시 95% 성공 Fallback | 실제 판별 오류는 실패·재촬영 처리, 샘플 버튼에서만 고정 성공 |
| 기술 클래스·신뢰도 비노출 | 구버전 클래스명이 기본 HTML·모델 계약에 남음 | 모델 계약을 3개 장소 ID로 교체하고 최종 결과 카드는 장소 성공/실패 중심으로 표시 |
| TimeTrace 공통 host | 간이 오버레이 화면만 존재 | 공통 host와 장소별 현재/과거 명칭 전환을 유지 |
| 조각 2 A+B 미션 | 덧붙인 화면은 있으나 상태 저장 없음 | 정확히 A+B 선택 시 통과하고 결과 상태 저장 |
| 조각 3 위조 기록 D 미션 | 덧붙인 화면은 있으나 상태 저장 없음 | D 선택 시 통과하고 결과 상태 저장 |
| 고정 웹툰 | 조각 1만 완성 4컷, 조각 2·3은 고정 참고 이미지 | 생성형 API 없이 로컬 고정 에셋만 사용. 조각 1은 4컷, 조각 2·3은 제공된 고정 이미지 연결 |
| 5초 영상 후 감정 대화 | 선택형 화면만 덧붙고 상태 저장 없음 | 3개 선택지·20자 입력·건너뛰기와 응답 저장 추가 |
| 상시 기록새 챗봇 | 중국어/해설 분기와 안내형 챗봇 일부 존재 | 한국어·중국어·중국인 맞춤 해설 유지, 촬영·Detection·TimeTrace·영상 화면에서는 챗봇 숨김 |
| 최종 퀴즈 B→C→A | 배열 UI는 존재 | 선택 순서 저장 및 B→C→A 검증 유지 |
| 예약이 아닌 다음 장소 확정 | `reservation` 문구와 상태가 노출 | 사용자 화면을 ‘다음 장소 확정’과 QR 이동 안내로 변경 |

## 검증 결과

- Python·JavaScript·JSON 문법 검사 통과
- 필수 HTML ID 중복 없음
- 생성형 웹툰 API 및 키 파일 없음
- 조각 1 4컷 고정 웹툰과 조각 2·3 고정 에셋 연결 확인
- 중국어 + 중국 문화권 경로: 언어 → 문화권 → 닉네임 → 기록새 → 조각 1 Detection까지 DOM 전환 확인
- 중국어 챗봇 열기·닫기 확인
- 조각 2 A+B 정답과 상태 저장 확인
- PTH 모델 3개 포함 확인

## 제공 자료상 남은 제한

1. 조각 2·3의 완성 4컷 웹툰 파일과 최종 장소 이야기 4개 세로형 완성 이미지가 압축본에 없습니다. 현재는 제공된 고정 참고 이미지를 연결했습니다. 완성 이미지를 같은 에셋 경로에 넣으면 런타임 경로만 교체하면 됩니다.
2. 별도 TimeTrace 모듈 파일이 제공되지 않아 공통 연결 host와 현재/과거 상태 전환까지만 유지했습니다. 실제 특징점 정합·이동 방향 안내는 TimeTrace 모듈을 해당 host에 연결해야 합니다.
3. 기록새 챗봇은 외부 AI API가 아닌 시연 가능한 안내형 로컬 챗봇입니다.
