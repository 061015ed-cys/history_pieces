# History Pieces 본선 1차 통합본 개선 보고서

- 기준일: 2026-07-17
- 대상: `History-Pieces-1st-Integration-0717`
- 비교 자료: 예선 HTML/CSS/JavaScript, 팀원별 제출 코드·문구·데이터, 초기 모바일 화면 시안, 수정 전 1차 통합본
- 원칙: 기능·디자인을 새로 만들지 않고 자료 반영 누락과 통합 문제만 수정

## ① 현재 코드 분석

수정 전 통합본은 단일 `appState`, `data-action` 이벤트 라우팅, 팀원별 모듈 연결점, 세 조각 반복 흐름, 생성형 AI Fallback, 추천 데이터까지 갖춰 1차 통합본의 기본 목적은 충족했다. 특히 예선 코드를 통째로 복사하지 않고 오버레이 경험과 미디어 처리 개념만 본선 구조에 옮긴 방향은 적절했다.

다만 자료를 다시 대조했을 때 다음 다섯 부분은 통합 과정에서 의미가 약해지거나 구조가 섞여 있었다.

1. 초기 시안의 `첫 기록 안내 → 장면 기록 카메라` 두 화면이 한 화면으로 합쳐져 있었다.
2. 예선에 있던 기록새 등장 팝 효과가 빠졌고, 로딩 돋보기는 위아래 이동이 아니라 크기 변화만 했다.
3. 팀원 제출 여정필름은 독립 모듈 규격이었지만 실제 통합본에서는 공통 `script.js`와 웹툰 렌더러에 역할이 분산되어 있었다.
4. 페이지가 직접 스크롤되는 기본 구조와 미사용 CSS가 남아 최종 시안을 씌울 때 화면별 예외가 늘어날 가능성이 있었다.
5. 예선의 다음 장소 후보 3개와 선택 과정이 통합본에서 검증 추천 1개 자동 표시로 축소되어 있었다.

## ② 발견한 문제

| 구분 | 발견 내용 | 영향 |
| --- | --- | --- |
| 시안 순서 누락 | 첫 기록 안내와 카메라 화면이 `first-record-page` 하나로 합쳐짐 | 시안 적용 시 한 화면을 다시 분리해야 함 |
| 설정 목록 | 내부 스크롤 속성은 있었지만 항목 수와 flex 높이 계약이 부족함 | 작은 iPhone에서 페이지 전체 스크롤로 번질 수 있음 |
| 기록새 모션 | 예선의 등장 팝 효과가 통합 과정에서 제외됨 | 시안에 명시된 등장 연출 미반영 |
| 로딩 모션 | 돋보기가 확대·축소만 함 | 시안의 상하 이동 로딩과 불일치 |
| 여정필름 중복 책임 | 클립 미리보기는 웹툰 모듈, 실제 합성은 공통 JS가 담당 | 함수 계약과 상태 소유권이 분산됨 |
| 여정필름 누락 | `journeyPlayClipsSequentially()` 공개 인터페이스가 없음 | 팀원 제출 규격과 1차 Fallback 시연이 불일치 |
| 합성 원본 오류 | 제출본의 고정 MP4/VP9 조합과 서로 다른 AudioContext 연결은 일부 브라우저에서 실패 가능 | 실제 합성 성공률 저하 |
| 스타일 결합 | AI 판정 색을 JavaScript 인라인 스타일로 지정 | 최종 디자인에서 상태 색을 CSS로 일괄 교체하기 어려움 |
| 미사용 CSS | 이전 2패널 웹툰용 클래스가 DOM 없이 남음 | 최종 웹툰 스타일 작업 시 혼동 가능 |
| 빈 이미지 `src` | `src="data:,"` 임시값이 다수 존재 | 불필요한 오류 상태와 자산 연결 혼동 |
| 추천 선택 흐름 축소 | 예선의 3개 후보·선택 후 예약 흐름이 추천 1개 자동 표시로 변경됨 | 사용자가 후보를 비교하거나 이전 목록으로 돌아갈 수 없음 |

## ③ 수정 이유

- 화면 분리는 새 기능 추가가 아니라 제공된 초기 시안과 예선의 촬영 흐름을 복원하는 작업이다.
- 여정필름은 팀원이 제출한 공개 함수명을 유지하면서 내부 구현만 한 모듈로 모아야 이후 담당자가 파일 하나만 교체할 수 있다.
- 모바일에서는 전체 페이지를 강제로 잘라내기보다 앱 셸 높이를 고정하고, 복잡한 화면만 안전하게 페이지 내부 스크롤을 허용하는 편이 안정적이다.
- 최종 디자인 전 단계에서는 색·이미지를 확정하기보다 상태와 레이아웃을 class로 노출하는 것이 유지보수에 유리하다.

## ④ 수정 계획

1. 기존 화면 ID와 `appState` 계약을 유지한다.
2. `first-record-page`를 안내 화면으로 유지하고 `first-record-camera-page`만 추가한다.
3. 설정 화면에 시안의 비활성 항목을 반영하고 목록 카드만 flex 영역 안에서 스크롤되게 한다.
4. 예선의 기록새 팝·오버레이 톤 전환을 현재 CSS 구조에 맞춰 복원한다.
5. 여정필름 저수준 로직을 독립 모듈로 옮기고 공통 JS는 화면 상태만 조정한다.
6. DOM 참조, 이벤트 라우팅, 구문, CSS, JSON, 한국·중국 분기와 전체 샘플 흐름을 회귀 검사한다.
7. 모바일 퍼스트 기준을 390×844로 명시하고 Android·태블릿·노트북까지 앱 셸과 스크롤을 실측한다.

## ⑤ 실제 수정 내용

### `index.html`

- 앱 루트를 의미 있는 `<main class="app-shell">`로 변경했다.
- 언어·국가 화면에 `settings-list-page` 구조 class를 추가했다.
- 시안에 있던 비활성 언어·국가 항목을 목록 안에 복원했다.
- `first-record-page`와 `first-record-camera-page`를 분리했다.
- 기록새 등장 화면에 키보드 접근용 `role`, `tabindex`를 추가했다.
- AI 결과 강조값에 `result-emphasis` class를 추가했다.
- 빈 `src="data:,"`를 제거했다.
- 여정필름 모듈을 공통 JS 다음에 로드하도록 연결했다.
- 예선의 다음 장소 후보 3개를 목록 화면으로 복원하고, 별도의 상세·간략 약도·뒤로가기·확정 화면을 추가했다.
- `viewport-fit=cover`와 `theme-color`를 선언해 모바일 WebView·앱 패키징 기반을 보강했다.

### `style.css`

- 앱 셸을 `100dvh` 기준으로 고정하고 각 페이지가 셸 내부에서만 동작하도록 정리했다.
- safe-area와 공통 page padding 토큰을 추가했다.
- 페이지를 flex 기반 가용 높이 구조로 바꾸고, 화면 밖 배경 장식이 가짜 스크롤 영역을 만들지 않도록 클리핑했다.
- 언어·국가 페이지는 페이지 스크롤을 막고 `.list-card`만 스크롤되게 했다.
- 중국인 모드는 팀원 원본의 적색 `#9f2f2f`·금색 `#d2a84b` 포인트 테마와 전용 배지·요약 색상을 유지했다.
- 비디오 기록 프레임과 안내 일러스트 슬롯을 viewport 높이에 맞춰 제한했다.
- 예선의 기록새 팝 효과와 부드러운 대사 영역 움직임을 복원했다.
- 돋보기 로딩을 상하 이동으로 변경했다.
- 예선 오버레이의 현재 화면 톤 전환을 복원했다.
- AI 성공·실패 색을 `data-result-state` 기반 CSS로 분리했다.
- 사용하지 않는 구형 웹툰 CSS 4종을 제거했다.
- 작은 높이 화면에서 제목·버튼·미디어·웹툰 패널을 압축하는 규칙을 보강했다.
- 390×844 모바일 퍼스트를 기준으로 휴대폰은 전체 폭, 태블릿·데스크톱은 최대 460px 중앙 앱 셸을 사용하도록 했다.
- 820px 이하의 짧은 화면에서만 조각 목록·오버레이·여정필름을 압축해 페이지 스크롤을 제거했다.

### `script.js`

- 화면 이동 시 활성 페이지 자체의 스크롤 위치도 초기화한다.
- 여정필름 화면 진입 순서를 `클립 적재 → 재생`으로 바로잡았다.
- 기록새 등장 화면에서 Enter·Space로도 다음 대사로 진행할 수 있다.
- AI 판정의 인라인 색 변경을 제거하고 카드 상태만 기록한다.
- 문구 데이터 key를 문화권처럼 보이던 `korea/china`에서 실제 역할인 `ko/zh-CN`으로 정리했다.
- 첫 기록 안내 문구와 카메라 안내 문구의 DOM 매핑을 분리했다.
- 여정필름 공통 JS는 진행 표시·Fallback 문구·다운로드 버튼 상태만 담당하도록 축소했다.
- 추천 후보 선택, 상세 진입, 후보 목록 복귀, 명시적 확정 예약을 공통 이벤트 라우터에 연결했다.
- 중국어 선택 시 정적 화면뿐 아니라 AI 판별 결과·Toast·Fallback·퀴즈·추천·예약 문구도 공통 다국어 사전을 사용하도록 연결했다.

### `modules/i18n.js`

- 기존 한국어 DOM 원문을 보존한 채 `zh-CN` 선택 시 35개 화면의 제목·설명·버튼·입력 안내·접근성 문구를 중국어로 전환한다.
- 구도훈 AI, 이지영 여정필름, 최유석 웹툰·추천 모듈이 같은 동적 메시지 사전을 사용하게 했다.
- 한국어를 다시 선택하면 최초 원문과 기존 `COPY_DATA`를 복원해 언어 전환이 한 방향으로 고정되지 않게 했다.
- 중국어 언어명에 `lang="zh-CN"`, 간체 글꼴 fallback, 명시적 굵기와 `font-synthesis: none`을 적용해 선택 전후 일부 글자만 굵어지는 현상을 차단했다.

### `modules/journey-film-0715.js`

- 팀원 제출 인터페이스 `journeySaveClipVideo()`, `journeyPlayClipsSequentially()`, `journeySynthesizeFinalFilm()`, `journeyConnectInstagramShare()`를 제공한다.
- 3개 영상을 한 플레이어에서 순차 반복 재생한다.
- 지원 가능한 MIME type을 런타임에서 선택한다.
- Canvas 720×1280, MediaRecorder, 단일 AudioContext로 최대 5초씩 합성한다.
- Object URL, MediaStream track, AudioContext, animation frame을 정리한다.
- 합성 불가·파일 누락·재생 실패 시 순차 재생으로 전환한다.
- 공통 `appState.journeyFilm`과 결과 상태를 동기화한다.

### `modules/yuseok-recommendation-0716.js`

- 최유석의 `verified: true` 추천 데이터와 공식 지도·상세 링크를 우선 유지한다.
- 검증 데이터가 3개보다 적을 때만 예선에 있던 후보명·태그·설명으로 수를 보완한다.
- 확인되지 않은 거리·주소·외부 URL은 만들지 않고 해당 외부 버튼을 숨긴다.
- 목록·선택·상세·복귀·확정 예약 상태를 모듈 안에서 관리하고 `appState`와 동기화한다.

## ⑥ 수정 후 검증

| 검사 | 결과 |
| --- | --- |
| JavaScript 6개 파일 구문 검사 | 통과 |
| CSS 파싱 | 통과 |
| JSON 3개 파일 파싱 | 통과 |
| HTML 파싱 | 통과 |
| 중복 HTML ID | 0개 |
| 누락된 `data-next` 대상 | 0개 |
| 처리되지 않는 `data-action` | 0개 |
| JavaScript가 참조하지만 없는 고정 DOM ID | 0개 |
| 한국어·한국 샘플 전체 흐름 | 시작부터 다음 기록 예약까지 통과 |
| 중국어·중국 설정 분기 | 중국 모드 소개 및 중국어 문구 적용 통과 |
| 중국어 정적 화면 전수 검사 | 35개 화면 표시 텍스트·alt·aria-label·placeholder에서 한글 잔존 0개 |
| 중국어 동적 전체 흐름 | AI 샘플 판별 3회 → 웹툰 3편 → 장소 이야기 → 기록 3개 → 추천 3곳 → 상세 → 예약까지 중국어 출력 확인 |
| 언어 복원 | 중국어 선택 후 한국어 재선택 시 35개 화면 원문과 문구 상태 복원 확인 |
| 중국어 언어명 굵기 | 선택 전후 동일 `lang`, 글꼴 fallback, `font-weight: 700`, 합성 굵기 비활성화 확인 |
| 조각 수집·5초 기록 상태 | 조각 3개, 기록 3개 저장 확인 |
| 여정필름 공개 함수 | 4개 인터페이스 노출 확인 |
| 추천 후보 흐름 | 후보 3개 → 두 번째 상세 → 목록 복귀 → 첫 번째 상세 → 확정 예약 통과 |
| iPhone SE(375×667)·iPhone 12/13/14 계열(390×844) | 35개 화면 페이지 스크롤 0개 |
| Android(360×800·412×915) | 35개 화면 페이지 스크롤 0개 |
| 태블릿(768×1024) | 460px 중앙 앱 셸, 35개 화면 페이지 스크롤 0개 |
| 노트북(1366×768) | 460px 중앙 앱 셸, 35개 화면 페이지 스크롤 0개 |
| 설정 목록 스크롤 | 모든 측정 크기에서 언어·국가 페이지만 목록 카드 내부 스크롤 |
| 중국인 모드 포인트 색 | `body[data-culture="china"]`에서 적색·금색 전용 accent 전환 확인 |
| 기록새 시안 인터랙션 | 팝 등장·대사 흔들림·전체 화면 터치·키보드 진행 확인 |

실제 iPhone Safari의 카메라 권한, MediaRecorder 코덱, 다운로드 동작은 최종 실기기에서 다시 검증해야 한다. 이번 검증은 DOM 흐름·상태·정적 구조·브라우저 API 미지원 Fallback까지를 대상으로 했다.

## ⑦ 자료 반영 재확인

| 자료 | 반영 결과 |
| --- | --- |
| 예선 코드 | 화면 전환 단순성, 기록새 팝, 과거·현재 오버레이, 다음 장소 후보 3개와 선택 흐름을 선별 반영 |
| 구도훈 코드 | AI 모듈 반환 계약, 샘플, 실패·재촬영, 영상 길이 검사 유지 |
| 최유석 코드 | 생성형 AI·4패널 렌더링·검증 추천·Fallback·모듈 로드 순서·중국 모드 적색/금색 포인트 유지 |
| 이지영 문구 | 기록새 톤, 미션·재촬영·5초 기록·여정필름 문구 유지 |
| 이지영 여정필름 | 기능명 접두사, 3클립 적재, 순차 재생, 합성, Instagram 연결 반영 |
| 초기 시안 | 표지→언어→국가→닉네임→첫 기록 안내→카메라→탐색→확인→기록새 순서와 구조 반영 |

## 팀원별 구현과 통합·개선 내역

| 팀원 | 제출·담당 구현 | 통합본 반영 | 개선 과정의 수정 |
| --- | --- | --- | --- |
| 이지영 | 기록새 대사 톤, 조각별 미션·재촬영·5초 기록 문구, 여정 완료·공유 문구, 3클립 여정필름 저장·순차 재생·합성·Instagram 연결 | 확정 문구 데이터, 기록새 화면, 조각 흐름, 여정필름 UI와 독립 모듈에 반영 | 분산된 여정필름 책임을 `journey-film-0715.js`로 통합하고 MIME 탐지·자원 정리·순차 재생 Fallback을 보강함 |
| 구도훈 | 직접 학습 AI 연결용 판별 인터페이스, 이미지 입력 검증, 성공·실패 결과, 재촬영, 5초 영상 검증 | `dohun-ai.js`, 조각 1~3 사진 입력·AI 결과·재촬영·샘플 진행·영상 기록 상태에 반영 | 반환 계약과 실패 흐름을 유지하면서 DOM 상태·버튼 잠금·CSS 결과 상태를 공통 구조에 맞게 정리함. AI label·판정 이유·오류 안내는 선택 언어를 따르도록 연결함. 실제 학습 모델은 제공되지 않아 시뮬레이션 상태를 유지함 |
| 최유석 | 생성형 AI 호출, 조각·장소 4패널 JSON 검증, 웹툰 렌더링, 한국/중국 Fallback, 문화권 분기, 검증 추천 데이터와 공식 URL | `yuseok-ai`, `yuseok-story-renderer`, `yuseok-recommendation`, Fallback JSON·source-facts에 반영 | 웹툰과 여정필름의 중복 책임을 분리하고, 검증 추천 1개를 우선 유지하면서 예선 후보 2개를 설명 전용으로 보완해 3개 후보 비교·상세·복귀·확정 흐름을 복원함. 중국어 선택 시 세 후보의 이름·태그·근거·도보 문구·예약 제목까지 중국어로 정규화함 |

## 수정하지 않은 항목과 잔여 위험

다음은 자료가 없거나 1차 통합 단계의 범위를 넘기 때문에 임의로 완성하지 않았다.

- 조각 1 실제 직접 학습 모델과 전처리 코드
- 첫 사진의 실제 장소 탐색 AI
- 조각 2·3의 출처가 확인된 옛 기록 이미지
- 실제 생성형 AI 백엔드 endpoint와 API 키 설정
- 최종 기록새·배경·웹툰 이미지 및 시안별 세부 디자인
- 누락된 샘플 사진·영상 파일
- 1897 개항문화거리·목포진 역사공원의 검증된 거리·공식 상세·외부 지도 URL

이 항목들은 현재 코드에서 성공한 기능처럼 위장하지 않고 Fallback 또는 미연결 상태로 유지했다. 현재의 `예약`은 외부 시설 예약이 아니라 다음 기록 장소를 앱 내부 여정에 확정하는 의미로 처리한다.
