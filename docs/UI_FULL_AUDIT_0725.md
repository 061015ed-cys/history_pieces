# History Pieces 2차 통합본 UI 전체 검수

## 검수 기준

- 업로드된 `HistoryPieces_2차 통합본_0725(2).zip`을 기준으로 작업했습니다.
- 공통틀의 `index.html`, `style.css`, `script.js`, 기존 `modules` 파일은 수정하지 않았습니다.
- 수정은 `addons/integrated-ui-0725.css`, `addons/integrated-ui-0725.js`에만 반영했습니다.
- 실제 서버를 실행해 `/api/health`, HTML add-on 주입, Python·JavaScript 문법을 확인했습니다.
- Gemini 과금 호출은 UI 검수에서 실행하지 않고, 승인된 Fallback 웹툰 데이터를 같은 렌더러에 주입해 레이아웃을 검사했습니다.

## 실행 검수

- `verify_integrated.py`: PASS
- 공통틀 원본 파일 12개 변경 없음
- HTML ID 147개, 중복 0개
- 조각 웹툰 승인 장면: 7 / 8 / 8개
- 장소 웹툰 승인 장면: 21개
- PTH 모델 파일 3개 확인
- 모델 런타임 의존성 정상
- Python 문법 검사 통과
- JavaScript 문법 검사 통과
- 서버 `/api/health` 정상 응답

## 화면 검수

### 전체 화면 시각 검수

- 데스크톱 1280×800에서 전체 34개 화면을 순서대로 렌더링하고 캡처했습니다.
- 조각 웹툰 1·2·3에서 `SCENE 01`부터 화면 상단에 정상 표시되는 것을 확인했습니다.
- 목포 안내 프로그레스 화면은 1/3, 2/3, 3/3 상태를 각각 확인했습니다.
- 다음 장소 상세 화면에서 `목포역 → 선택한 추천 장소` 연결선이 두 노드 사이에 표시되는 것을 확인했습니다.
- 검수 캡처는 `docs/ui-audit` 폴더에 있습니다.

### 반응형 검수

- 360×740 모바일 화면에서 한국어·중국어 주요 화면을 확인했습니다.
- 320×568 소형 화면에서 기존에 상단이 잘리던 장소 확인·장소 이야기 해금·예약 화면을 재검사했습니다.
- 내용이 화면 높이를 넘는 경우에만 `hp-overflow-layout`이 자동 적용되어 상단부터 스크롤됩니다.
- 하단 콘텐츠는 스크롤로 끝까지 접근할 수 있습니다.

## 최종 수정 내용

1. 조각 웹툰 상단 `SCENE 01·02` 잘림 방지
2. 긴 화면의 중앙 정렬 때문에 제목이 위로 밀리는 문제 자동 감지 및 보정
3. 중국인 안내, 닉네임, 첫 기록, 여정필름, 추천 장소 상세 화면 상단 정렬 보정
4. 프로그레스바 1/3·2/3·3/3 연결 상태 유지
5. 조각 2·3 진입 전에도 동일한 진행 화면 표시
6. 장소 후보의 물음표를 깃털펜 `🪶`으로 변경
7. 표시되는 기록새 이미지도 깃털펜 아이콘으로 통일하여 깨진 이미지 제거
8. 추천 장소 약도를 `목포역 → 추천 장소` 구조로 정렬
9. 하단 버튼 문구와 화살표 겹침 방지

## 참고

`next-place-page`의 추천 설명은 기존 디자인의 한 줄 요약 `line-clamp`를 유지했습니다. 이는 글자 잘림 오류가 아니라 후보 카드 높이를 맞추기 위한 기존 의도입니다.
